# YASB GUI Registry

This repository serves as the remote registry/catalog for [YASB GUI](https://github.com/amnweb/yasb-gui).

It contains the official list of supported widgets that the GUI application uses to present options to the user.